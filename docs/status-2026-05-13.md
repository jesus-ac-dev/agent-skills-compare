# Estado do projecto — 2026-05-13

Snapshot do que está feito e do que falta, organizado para retomar
sem ter de reler toda a conversa anterior.

## Já feito (mais recente primeiro)

| Commit  | O quê                                                                                                                                                          |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1e6ce2c | **Classifier code-aware:** prompt branches por kind (markdown/code/config/text), `file_types` ganha `javascript/typescript/python/shell`, helper `fileKind.js` |
| 5ee968b | UI: wrap `DropdownMenuLabel` em `DropdownMenuGroup` (fix do `MenuGroupRootContext is missing`)                                                                 |
| bd7e9ed | UI: mount `<ProviderSelect />` no header                                                                                                                       |
| ddec58d | UI: criação do componente `ProviderSelect`                                                                                                                     |
| 1786a64 | Curated: re-aplicação de `name` NOT NULL + prioridade + ordem agentes-primeiro                                                                                 |
| 02868d1 | (Jules) Refactor: `QuotaError` partilhado, dedup por hash, broaden file discovery                                                                              |

## A fazer (lista priorizada)

1. **Search (full-text + embeddings)** — meta declarada do projecto é "BD pesquisável de skills". Hoje só há filtros por eixo.
   - Full-text via `tsvector` em `summary` + `use_cases` (~2h).
   - Embeddings via `pgvector` para semantic search (~½ dia).
2. **Higiene operacional**
   - `npm run db:health` — repos por status, ficheiros sem análise, top erros recentes, fragmentação por activity/tag. (Posso fazer já — ~50 LOC.)
   - `npm run db:canonicalize` — aplica mapa de aliases (`config/canonical-aliases.json`). **Mapa é judgment do user**; código é meu.
   - Re-classify selectivo quando se troca de LLM (hoje o hash skip impede; precisa de invalidar `analysis` para repos curated).
3. **Refresh + visibilidade de erros**
   - Mostrar idade dos dados (`last_processed_at`) na UI + botão "reanalisar".
   - Surfaceiar `files_sources.status` (pending/processing/completed/reused/skipped/error) por repo na UI.
4. **Manual repo add via UI** — botão no header → POST com URL → insere em `repos` com `status='pending'`. ~30 LOC.

## Ordem prática sugerida

1. Validar #1 com um run real do pipeline (precisa de quota Claude CLI ou troca para Groq).
2. `db:health` em paralelo (não bloqueia nada).
3. Manual repo add (pequeno, payoff imediato).
4. Search/embeddings (maior, beneficia de design próprio).
5. Visibilidade de erros + refresh (UI work).

## Decisões em aberto

- **Embedding model**: `text-embedding-3-small` (OpenAI), `all-MiniLM-L6-v2` (local), ou Voyage/Cohere? Dimensões 1536 vs 384 mudam o pgvector setup.
- **Aliases canonical map**: `config/canonical-aliases.json` ainda não existe; espera pelo user.
- **Re-classify cross-LLM**: clear `analysis.id` para repo X? Ou novo status `stale`? Decisão de UX, não técnica.

## Quirks descobertos nesta sessão

- `repos.name` é NOT NULL — qualquer seed/upsert precisa de passá-lo.
- `findResumableRepos` sort: `processing` primeiro, `pending` arbitrário. Curated novo entra na cauda sem prioridade especial — daí o `curatedUrls` + loop explícito em `main()`.
- Postgres IDENTITY sequences contam inserts falhados → gaps nos IDs (cosmético, não bug).
- shadcn `DropdownMenuLabel` usa `MenuPrimitive.GroupLabel` que exige `<Menu.Group>` parent.
- IDs case-sensitive em URLs de GitHub (`Kilo-Org` vs `kilo-org`) precisam de normalização lowercase.
- Worktree partilha portas do Supabase local — não correr `db:reset` no worktree.
